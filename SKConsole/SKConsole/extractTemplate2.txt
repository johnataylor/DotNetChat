From the email thread:
```
{{$input}}
```

Using the following structure:

{
  "summary": "", // Summarize the conversation in the email thread, identifying main points of discussion and any conclusions that were reached. Do not incorporate other general knowledge. Summary is in plain text, in complete sentences, with no markup or tags.
  "name": "", // The subject of the email thread
  "priority": "", // one of {{$Priorities}} depending on the ugency expressed in the email
  "workOrderType": "", // one of {{$WorkOrderTypes}} or NA if this cannot be determied
  "incidentType": "", // one of {{$IncidentTypes}} or NA if this cannot be determied
  "customerNames": [] // the To and From in the emails
}

Extract a JSON object:
