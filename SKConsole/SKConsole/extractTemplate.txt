Using these inputs
WorkOrderType comma separated list:
{{$WorkOrderTypes}}

Priority comma separated list:
{{$Priorities}}

IncidentType comma separated list:
{{$IncidentTypes}}

"workOrderType","priority","incidentType": match only one of entry, in case no option is matched use NA

Create a valid JSON document into English following this format:
{{$WorkOrderTemplate}}

Replace content of "summary" with {{ConversationSummarySkill.SummarizeConversation $input}}

From:
BEGIN CONTENT TO EXTRACT JSON OBJECT
{{$input}}
END CONTENT TO EXTRACT JSON OBJECT

Extract a JSON object, response must be a valid JSON, remove any xml tags at the end: